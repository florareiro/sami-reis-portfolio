import React from "react";
import { useAppContext } from "../../hooks/AppContext";

const Hero = () => {
  return (
    <div className={`mt-[80px] bg-hero bg-cover bg-center bg-no-repeat h-80 md:h-screen`}>
      {/* <img src="./images/hero/yin-und-bird-v2.gif" className="h-screen" alt="" /> */}
    </div>
  );
};

export default Hero;
